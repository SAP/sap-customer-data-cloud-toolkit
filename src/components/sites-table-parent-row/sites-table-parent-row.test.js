import React from 'react'
import { shallow } from 'enzyme'

// import ParentSiteTableRow from './sites-table-parent-row.component'

it('shoud render', () => {
	// TODO
	// expect(shallow(<ParentSiteTableRow/>).length).toEqual(1)
})
